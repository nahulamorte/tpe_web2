<?php require 'templates/layout/header.phtml' ?> 
<?php require 'templates/form.categorias.phtml' ?>

<div class="container mt-5">
    <h1 class="text-center mb-4">Categorias</h1>

    <div class="row">
        <?php foreach ($categorias as $categoria): ?>
            <div class="col-md-6 col-lg-4 mb-4">
                <div class="card shadow-sm h-100">
                    <div class="card-header bg-primary text-white text-center">
                        <h5 class="card-title mb-0"><?= htmlspecialchars($categoria->nombre); ?></h5>
                    </div>

                    <div class="card-body d-flex flex-column">
                        <p class="card-text text-muted">
                            <?= nl2br(htmlspecialchars($categoria->descripcion)); ?>
                        </p>
                    </div>

                    <div class="card-footer d-flex justify-content-around">
                        <!-- Botón para ver productos -->
                        <a href="categoriaitems/<?= $categoria->id_categoria; ?>" class="btn btn-outline-primary btn-sm">
                            Ver productos
                        </a>
                        <!-- Botón para eliminar la categoría -->
                        <a href="categorias/eliminar/<?= $categoria->id_categoria; ?>" class="btn btn-outline-danger btn-sm">
                            Eliminar
                        </a>
                    </div>
                </div>
            </div>
        <?php endforeach; ?>
    </div>
</div>

<?php require 'templates/layout/footer.phtml'; ?>
