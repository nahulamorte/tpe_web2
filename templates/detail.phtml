<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalle del Producto</title>
    <!-- Incluye Bootstrap o cualquier otro CSS que estés usando -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1>Detalles del Producto</h1>
        <div class="card">
            <div class="card-body">
                <h2 class="card-title"><?= htmlspecialchars($product->nombre); ?></h2>
                <p class="card-text"><strong>Descripción:</strong> <?= htmlspecialchars($product->descripcion); ?></p>
                <p class="card-text"><strong>Precio:</strong> $<?= htmlspecialchars($product->precio); ?></p>
                <p class="card-text"><strong>Peso Neto:</strong> <?= htmlspecialchars($product->peso_neto); ?> kg</p>
                <p class="card-text"><strong>Fecha de Empaquetado:</strong> <?= htmlspecialchars($product->fecha_empaquetado); ?></p>
                <p class="card-text"><strong>Fecha de Vencimiento:</strong> <?= htmlspecialchars($product->fecha_vencimiento); ?></p>
                <p class="card-text"><strong>Stock Disponible:</strong> <?= htmlspecialchars($product->stock); ?></p>
                <a href="/productos" class="btn btn-primary">Volver al Listado</a>
            </div>
        </div>
    </div>

    <!-- Incluye Bootstrap JS si lo necesitas -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
