<?php require 'templates/layout/header.phtml' ?>

<div class="container mt-5">
    <div class="row">
        <div class="col-lg-8">
            <h1 class="text-center mb-4">Categorías</h1>

            <div class="row">
                <?php foreach ($categorias as $categoria): ?>
                    <div class="col-md-6 col-lg-6 mb-4">
                        <div class="card shadow-sm h-100">
                            <div class="card-header bg-primary text-white text-center">
                                <h5 class="card-title mb-0"><?= $categoria->nombre; ?></h5>
                            </div>
                            <div class="card-body d-flex flex-column">
                                <p class="card-text text-muted">
                                    <?= $categoria->descripcion; ?>
                                </p>
                            </div>
                            <div class="card-footer d-flex justify-content-around">
                                <a href="categorias/<?= $categoria->id_categoria; ?>" class="btn btn-outline-primary btn-sm">Ver productos</a>
                                <?php if ($this->user): ?>
                                    <a href="categorias/eliminar/<?= $categoria->id_categoria; ?>" class="btn btn-outline-danger btn-sm">Eliminar</a>
                                    <a href="categorias/actualizar/<?= $categoria->id_categoria; ?>" class="btn btn-outline-warning btn-sm">Editar</a>
                                <?php endif; ?>
                            </div>
                        </div>
                    </div>
                <?php endforeach; ?>
            </div>
        </div>

        <?php if ($this->user): ?>
            <div class="col-lg-4">
                <h2 class="text-center mb-4">Agregar Categoría</h2>
                <form action="categorias/agregar" method="post" class="p-4 shadow rounded bg-light">
                    <div class="mb-3">
                        <label for="nombre" class="form-label">Nombre</label>
                        <input type="text" class="form-control" id="nombre" name="nombre" placeholder="Ingresa el nombre" required>
                    </div>
                    <div class="mb-3">
                        <label for="descripcion" class="form-label">Descripción</label>
                        <textarea class="form-control" id="descripcion" name="descripcion" rows="3" placeholder="Ingresa una descripción" required></textarea>
                    </div>
                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary btn-lg">Agregar</button>
                    </div>
                </form>
            </div>
        <?php endif; ?>
    </div>
</div>

<?php require 'templates/layout/footer.phtml' ?>
