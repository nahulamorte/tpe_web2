<?php require 'templates/layout/header.phtml' ?>

<div class="container mt-5">
    <h1 class="text-center mb-4">Productos Disponibles</h1>

    <!-- Iteración de productos -->
    <div class="row">
        <?php foreach ($productos as $producto): ?>
            <div class="col-md-6 col-lg-4 mb-4">
                <div class="card shadow-sm h-100">
                    <!-- Encabezado del producto -->
                    <div class="card-header bg-primary text-white text-center">
                        <h5 class="card-title mb-0""><?= htmlspecialchars($producto->nombre); ?></h5>
                    </div>

                    <!-- Cuerpo del producto -->
                    <div class="card-body d-flex flex-column">
                        <p class="card-text text-muted"><strong>Precio:</strong> $<?= htmlspecialchars($producto->precio); ?></p>
                        <p class="card-text text-muted"><strong>Peso Neto:</strong> <?= htmlspecialchars($producto->peso_neto); ?> kg</p>
                        <p class="card-text text-muted">
                            <strong>Descripción:</strong> <?= htmlspecialchars($producto->descripcion); ?>
                        </p>
                    </div>

                    <!-- Pie de la tarjeta con enlaces de acción -->
                    <div class="card-footer d-flex justify-content-around">
                        <!-- Enlace para ver detalles del producto -->
                        <a href="productos/<?= $producto->id_producto; ?>" class="btn btn-outline-primary btn-sm">
                            Ver Detalles
                        </a>
                        <br>
                        <!-- Enlace para actualizar el producto -->
                        <a href="productos/editar/<?= $producto->id_producto; ?>" class="btn btn-outline-warning btn-sm">
                            Actualizar
                        </a>
                        <br>
                        <!-- Enlace para eliminar el producto -->
                        <a href="productos/eliminar/<?= $producto->id_producto; ?>" class="btn btn-outline-danger btn-sm">
                            Eliminar
                        </a>
                    </div>
                </div>
            </div>
        <?php endforeach; ?>
    </div>
</div>

<?php require 'templates/layout/footer.phtml'; ?>
